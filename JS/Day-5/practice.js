//Today we learn about ASCII and how we will use it

let char = "AB"; //This is a character and it is represented by a number in ASCII

console.log(char.charCodeAt(1));

console.log(String.fromCharCode(99))//This will return the character represented by the number

//The ascii values can be used to perform various operations on strings, such as sorting, comparing, and manipulating characters. For example, we can compare two strings by comparing their ASCII values. We can also sort an array of strings by their ASCII values. Additionally, we can manipulate characters by using their ASCII values, such as converting uppercase letters to lowercase letters or vice versa.